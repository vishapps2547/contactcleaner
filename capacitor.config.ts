import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vishapps.contactcleaner',
  appName: 'Contact Cleaner',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
